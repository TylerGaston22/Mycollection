# components/figma/

Utility components specific to the Figma Make environment.

| File | Purpose |
|------|---------|
| `ImageWithFallback.tsx` | Image component that renders a `<img>` tag with a fallback placeholder if the image fails to load. Used wherever poster or thumbnail images are displayed |
