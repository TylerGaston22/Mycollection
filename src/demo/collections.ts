/**
 * Mock collections – empty seed data for custom tabs and sections.
 * Provides the initial (empty) state so the demo account starts
 * without any user-created organisational structure.
 */
import { CustomTab, CustomSection } from "../types";

export const mockCustomTabs: CustomTab[] = [];

export const mockCustomSections: CustomSection[] = [];
