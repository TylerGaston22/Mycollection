<?php
  // Author: 	    Sydney and Tyler
  // File: 		  	insertWishLists.php
  // Date:		    April 19, 2026
  // Class:		    CS 445	
  // Project: 	  BookstoreDB
  // Description: insert wishlists from reading a file

require_once("connDB.php");

$conn = db_connect();

if ($argc < 2) {
	exit("Error: missing file\n");
}

if (($handle = fopen($argv[1], "r")) !== FALSE) {

	/* skip header */
	fgetcsv($handle, 1000, ",", escape: "\\");

	while (($data = fgetcsv($handle, 1000, ",", escape: "\\")) !== FALSE) {

		$fName = $data[0];
		$lName = $data[1];
		$wishListName = $data[2];

		$sqlString = "CALL addWishList(:fname, :lname, :wishListName)";
		$sth = $conn->prepare($sqlString);
		$sth->bindValue(":fname", $fName);
		$sth->bindValue(":lname", $lName);
		$sth->bindValue(":wishListName", $wishListName);
		$sth->execute();
		$sth->closeCursor();

		for ($i = 3; $i < count($data); $i += 4) {
			if (isset($data[$i], $data[$i + 1], $data[$i + 2], $data[$i + 3]) &&
				$data[$i] !== "" && $data[$i + 1] !== "" && $data[$i + 2] !== "") {

				$sqlString = "CALL addWishListItem(:fname, :lname, :wishListName, :title, :editionName, :quantity, :note)";
				$sth = $conn->prepare($sqlString);
				$sth->bindValue(":fname", $fName);
				$sth->bindValue(":lname", $lName);
				$sth->bindValue(":wishListName", $wishListName);
				$sth->bindValue(":title", $data[$i]);
				$sth->bindValue(":editionName", $data[$i + 1]);
				$sth->bindValue(":quantity", $data[$i + 2]);
				$sth->bindValue(":note", $data[$i + 3]);
				$sth->execute();
				$sth->closeCursor();
			}
		}
	}
	fclose($handle);
}

db_close($conn);

?>